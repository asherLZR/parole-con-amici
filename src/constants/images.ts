import { Language } from './tile-defaults';

const FLAGS_FOLDER = 'flags/';

export const FLAG_ICONS: Record<Language, string> = {
	[Language.ENGLISH]: `${FLAGS_FOLDER}great-britain-50.png`,
	[Language.ITALIAN]: `${FLAGS_FOLDER}italy-50.png`,
};
